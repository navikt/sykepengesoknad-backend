package no.nav.helse.flex.controller.domain.sykepengesoknad

enum class RSSykmeldingstype {
    AKTIVITET_IKKE_MULIG,
    GRADERT,
    BEHANDLINGSDAGER,
    AVVENTENDE,
    REISETILSKUDD,
    UKJENT,
}
